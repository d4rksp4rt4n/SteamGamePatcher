# Core App Dependencies
requests>=2.31.0
Pillow>=10.0.0
gdown>=4.7.1
vdf>=3.5
python-docx
PyMuPDF  # Imported as 'fitz'

# Database Build Scripts
google-api-python-client>=2.108.0
google-auth>=2.23.0
fuzzywuzzy>=0.18.0
python-levenshtein>=0.25.0  # Speeds up fuzzywuzzy

# Optional (Windows-specific for app/indexer)
# pywin32>=306  # Uncomment if needed for file locking/registry
